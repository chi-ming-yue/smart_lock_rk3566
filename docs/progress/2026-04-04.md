# 2026-04-04

## Done

- Created `smart_lock_rk3566` as a new Git repository
- Imported the working controller, kernel module, and face-recognition baseline
- Added top-level project README
- Extracted 5-second camera awake policy into `modules/camera_runtime/`
- Updated imported controller main loop to use the extracted lifecycle module
- Updated hardware motion polling to avoid `lseek` on `/dev/test`
- Verified top-level controller build with `cmake -S . -B build-board-key -DCMAKE_TOOLCHAIN_FILE=cmake/rk3566_toolchain.cmake`
- Verified `total_controller` and `total_keypad_test` build from the new repository path
- Verified `drivers/week3_kernel/build/mydev.ko` and userspace test binaries build from the new repository path

## Pending

- Make the first repository commit
- Split database matching and interaction composition into dedicated modules
